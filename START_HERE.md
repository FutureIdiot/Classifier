# 双击启动

macOS 当前机器：

- 双击 `start.command`

Windows：

- 双击 `start_windows.bat`

第一次启动时，如果没有 `.env`，启动器会自动从 `.env.example` 创建 `.env`。你可以在 Web UI 右上角齿轮配置里填写 `GEMINI_API_KEY`。

启动成功后打开：

```text
http://localhost:8080
```
